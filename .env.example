# Configuration de l'environnement - StockLine
# Copier ce fichier en .env et remplir les valeurs

# Environnement (development | production)
NODE_ENV=production

# Port de l'application (Render utilise 10000 par défaut)
PORT=10000

# Base de données PostgreSQL (fournie automatiquement par Render)
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://user:password@host:5432/stockline

# Secret pour les sessions Express (généré automatiquement par Render)
# Pour générer localement: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=votre-secret-tres-securise-minimum-32-caracteres
